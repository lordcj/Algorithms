void countingsort(lo n){
    // ve is a vector, arr is globally intitalised
    lo ma=-1;
    REP(0,n){
        coun[arr[i]]++;
        ma = max(ma,arr[i]);
    }
    REP(0,ma+1){
        while(coun[i]--)ve.pb(i);
    }
}
